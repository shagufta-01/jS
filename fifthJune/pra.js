function displayPerson({fname, lname, age, branch}){
console.log(`name:${fname} ${lname} is age:${age}, ${branch}`)
}
const person1 ={
    fname:"shagufta",
    lname:"fatima",
    age:21,
    branch:"computer science and eng..."
}
displayPerson(person1)